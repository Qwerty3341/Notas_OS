Se coloca `git remote -v` 
Si el origin sale como `https:...` lo cambiamos a ssh

Se aplica el comando:
```
git remote set-url origin git@github.com:usuario/repositorio.git
```
El nombre se puede sacar desde github en la sección clone por ssh
